-- Test copies may enable deterministic auto-start without changing release play.
return {
  auto_start = false,
  section = 1,
  difficulty = nil,
  all_enemies = false,
  boss_kind = nil,
  boss_victory = false,
  seed_sweep = 0,
  family_presentation = false,
  capacity_probe = false,
  flow_probe = false,
  tutorial_probe = false,
  player_trace = false,
  interaction_probe = false,
  failure_probe = false,
  section_exit_probe = false,
  menu_probe = false,
}
