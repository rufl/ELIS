# ELIS Backlog Archive

Completed, retired, and historical planning rows belong here rather than in `BACKLOG.md`.

## Archived Work

- [x] [status:green] [truth:proof-gated] Add active-layer rectangle selection and reusable stamps that preserve smart-terrain semantics, clip at map edges, transform without mutating source cells, and remain undoable. Proof: `zig test src/studio/model.zig`; `bash scripts/test_studio.sh`; `bash scripts/studio_smoke.sh`.
- [x] [status:green] [truth:proof-gated] Add a bounded typed entity grid and inspector with integer fields, undo/redo, collision warnings, schema-v3 persistence, v2 migration, and reserved metadata export. Proof: `zig test src/studio/model.zig`; `bash scripts/test_studio.sh`; `bash scripts/studio_smoke.sh`.
- [x] [status:green] [truth:proof-gated] Add line and outline/filled rectangle gestures with previews, erase mode, controller/keyboard operation, and one-command undo; add session visibility and painting locks for visual layers. Proof: `zig test src/studio/model.zig`; `bash scripts/test_studio.sh`; `bash scripts/studio_smoke.sh`.
- [x] [status:green] [truth:proof-gated] Add bounded map resize/rebase with nine anchors, preflight clipping diagnostics, semantic preservation, smart-edge refresh, and unified undo/redo. Proof: `zig test src/studio/model.zig`; `bash scripts/test_studio.sh`; `bash scripts/studio_smoke.sh`.
- [x] [status:green] [truth:proof-gated] Add project-defined schemas for the four entity slots with bounded names, up to four typed fields, defaults/bounds, v3 migration, richer inspection, and deterministic export metadata. Proof: `zig test src/studio/model.zig`; `bash scripts/test_studio.sh`; `bash scripts/studio_smoke.sh`.
- [x] [status:green] [truth:proof-gated] Add user-facing entity schema authoring for names, fields, kinds, defaults, and bounds with bounded text input and unified undo/redo. Proof: `zig test src/studio/model.zig`; `bash scripts/test_studio.sh`; `bash scripts/studio_smoke.sh`; isolated keyboard/mouse schema save/export inspection.
- [x] [status:green] [truth:proof-gated] Add Blank, Platformer, Arena, and Puzzle project templates with CLI selection, an undoable Workshop panel, preserved dimensions/tilesets, and deterministic content. Proof: `zig test src/studio/model.zig`; `bash scripts/studio_smoke.sh`; isolated keyboard template apply/save/export inspection.
- [x] [status:green] [truth:proof-gated] Add fail-closed Lupi export preflight for spatial validity, exact manifest-backed files, official 49,152-pixel tileset limits, referenced tile bounds, sparse visual tables, and bounded per-call tile sampling work. Proof: `bash scripts/test_studio.sh`; `bash scripts/studio_smoke.sh`; isolated 960x600 Lupi-safe status inspection.
- [x] [status:green] [truth:proof-gated] Align ELIS with the sourced physical-console profile: Lua 5.4, 4 MiB game heap, 16 MiB release limits, ESP32-S3/RP2350 diagnostics, RGB555, current clear/flip/stat semantics, three player slots, and official per-layer map exports. Proof: `bash scripts/runtime_smoke.sh`; `bash scripts/parity_smoke.sh`; `bash scripts/studio_smoke.sh`.
- [x] [status:green] [truth:proof-gated] Make Workshop's Lupi-safe admission profile fail closed for generated Lua data as well as assets/rendering: sparse semantic tables, 4,096 weighted entries, and 128 KiB generated source. Proof: 23 model tests and `bash scripts/studio_smoke.sh`.
