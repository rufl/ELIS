-- Test copies may enable deterministic auto-start without changing release play.
return {
  auto_start = false,
  section = 1,
  difficulty = nil,
  all_enemies = false,
  boss_kind = nil,
  boss_victory = false,
  seed_sweep = 0,
}
